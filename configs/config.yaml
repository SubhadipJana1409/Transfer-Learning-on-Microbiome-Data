data:
  n_source: 800
  n_target: 120
  seed:      42

autoencoder:
  hidden_dims: [128, 64]
  latent_dim:  32
  lr:          0.001
  epochs:      80
  batch_size:  64

transfer:
  epochs:      80
  lr:          0.0005
  train_sizes: [8, 15, 25, 40, 60, 80]
